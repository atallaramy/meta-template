# Pending Verifications

> **The §15.3 tracked surface — an uncapped ledger, deliberately outside `STATUS.md`.**
> Time-deferred acceptance criteria from **done** tickets (GOVERNANCE §15.3): each row names
> ticket · AC · trigger · verification command. When the trigger fires and verification passes:
> tick the AC on the done ticket **and** remove the row here — same commit. Validator-enforced by
> `scripts/build_index.py` (a done ticket with a `[ ]` AC that is absent from this file fails the
> commit). This file legitimately grows and is **not** subject to the `STATUS.md` snapshot cap
> (GOVERNANCE §18) — that is exactly why it lives here and not in the snapshot.

## Pending Verifications

| Ticket | AC | Trigger | Verification | Status |
| ------ | -- | ------- | ------------ | ------ |
